---
name: showroom-skills
description: >-
  Install showroom skills, agents, prompts, templates, and docs from the
  RHDP Skills Marketplace repository into the project's .claude/ directory
  (resolved via the git repository root).
allowed-tools: Bash
model: haiku
disable-model-invocation: true
---

Run the script at `scripts/showroom-skills.sh` relative to this skill's directory.

Use the Bash tool to execute the script and report success or failure to the user.
